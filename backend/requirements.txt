# Core backend API
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.9
orjson==3.10.7
httpx==0.27.2

# Optional local vLLM serving and model-download tooling
# The model weights themselves are downloaded separately from Hugging Face.
vllm==0.17.0
huggingface_hub==0.36.2

# Local test runner
pytest==8.3.3

# Voice synthesis for coach playback
# edge-tts is the preferred neural voice path.
# pyttsx3 is the local fallback when edge-tts is unavailable.
edge-tts==7.2.7
pyttsx3==2.99
